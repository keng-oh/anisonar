annict_recent_sync:
  class: AnnictRecentSyncJob
  cron: "0 6 * * *"
  queue: annict_sync

annict_backfill:
  class: AnnictBackfillJob
  cron: "0 * * * *"
  queue: annict_sync

cover_image_bulk_resolve:
  class: CoverImageBulkResolveJob
  cron: "30 6 * * *"
  queue: default

# コンテナはTZ=Asia/Tokyoのため、cronはJSTで解釈される（db_dumpの明示は冗長だが安全のため残す）
db_dump:
  class: DbDumpJob
  cron: "0 4 * * * Asia/Tokyo"
  queue: default
